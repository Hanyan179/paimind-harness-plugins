import { PAIMIND_AGENT_PROFILE_REMOTE_DESCRIPTORS } from './remote.js'
export const TYPERT = Object.freeze({
  package: '@hansen/agent-builder', face: 'host', schemas: Object.freeze([]),
  invocations: PAIMIND_AGENT_PROFILE_REMOTE_DESCRIPTORS,
  model: Object.freeze({ services: Object.freeze([]), events: Object.freeze([]), objects: Object.freeze([]) }),
})
export default TYPERT
