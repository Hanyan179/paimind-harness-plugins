/** Browser-safe identity for dedicated native Harness Agent authoring sessions. */
export const AGENT_AUTHORING_SESSION_PREFIX = 'paimind-authoring-'

/** The only model-callable capability inside a PAIMind Agent authoring Session. */
export const AGENT_AUTHORING_PROPOSAL_TOOL = 'paimind_propose_agent_draft'
