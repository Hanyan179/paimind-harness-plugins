/** Host half of the Paramont identity plugin. */
export const name = 'paimind-branding'

/** The host half is intentionally empty; the visible contribution is client-only. */
export function apply(): void {}
