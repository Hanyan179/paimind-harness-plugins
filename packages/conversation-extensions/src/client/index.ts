/** Native Harness packages own every FP03 surface; this client half adds no slot. */
export const inject: readonly string[] = []

/** Composition marker only. Keeping this empty prevents duplicate conversation state and UI. */
export function apply(): void {}

