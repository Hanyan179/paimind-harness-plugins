import { MCP_REMOTE_DESCRIPTORS } from './remote.js'
export const TYPERT = Object.freeze({ package: '@hansen/mcp-center', face: 'host', schemas: Object.freeze([]), invocations: MCP_REMOTE_DESCRIPTORS, model: Object.freeze({ services: Object.freeze([]), events: Object.freeze([]), objects: Object.freeze([]) }) })
export default TYPERT
