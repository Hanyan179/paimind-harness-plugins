/** Host half of the FP02 UI plugin. */
export const name = 'paimind-launcher'

/** The host half is intentionally empty; the browser contribution is discovered through `dsh.client`. */
export function apply(): void {}

