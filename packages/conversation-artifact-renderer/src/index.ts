/** Host half of the conversation Artifact renderer; all behavior is client-side. */
export const name = 'paimind-conversation-artifact-renderer'

/** The package projects native Deliverables and owns no Host runtime state. */
export function apply(): void {}
