# Baseline Remediation — Final Acceptance

## Scope and preservation

- Repository baseline: `main` at
  `4fae86b9230d9e76317f3c8025abaed81038879f`.
- The original 48 tracked, unstaged user modifications are preserved outside
  the repository in the recovery pack recorded by the
  [P0 baseline](2026-08-16-baseline.md). Bundle verification, clean-clone patch
  restoration, file count, path list and copied-content comparisons passed.
- No commit, push, publish, credential change, production change or upstream
  source edit was performed.

## Governance and cleanup

- The sole normative authoring rules are the
  [Plugin Authoring Standard](../../standards/plugin-authoring.md); architecture,
  migration history, selected versions, roles and retirement each have one
  separate authoritative document.
- The one-time suite result is 30 workspace packages: 25 runtime packages (19
  client and six headless) plus five shared-support packages.
- Five approved zero-consumer identities were removed by package identity. The
  retained platform Scheduler remains at `packages/scheduler`. Exact identities
  and durable dispositions exist only in the
  [Package Retirement Ledger](../../migration/package-retirement-ledger.md).
- Effective integration examples moved to
  [`examples/platform-integration/`](../../../examples/platform-integration/)
  and passed three focused tests plus an independent TypeScript build.
- The protected [API snapshot](api-snapshot.json) covers all 30 retained
  packages and rejects unapproved changes to exports, Harness discovery,
  generated declarations, runtime entries or the bundle patch.

## Fast Gate

Reproduction command: `pnpm check`.

The final run passed:

- package identity, role, consumer and production-reachability compliance;
- TypeScript project checking;
- 65 test files and 206 tests;
- production declaration, Node ESM and Harness client builds;
- 30/30 API snapshot comparisons;
- 30 `npm pack --dry-run` audits with no source, test, build-metadata,
  local-runtime or credential pollution;
- strict `publint` for all 30 packages, with only the documented exact Harness
  Web-loader client-format exception;
- a NodeNext external consumer resolving 89 public JavaScript/type exports;
- migrated-example, 19-client framework and 84-Markdown-link checks.

## Release Gate

The real composition used the exact
[Compatibility Matrix](../../compatibility/matrix.md):

- `@deepseek-ai/dsh@0.1.0-rc.6`;
- `dsh-better-sidebar@0.12.2`;
- Office Viewer `0.1.0`.

`verify-harness-composition.mjs` passed real disposable-profile initialization,
provider-first installation, full PAIMind Bundle installation, configuration
dump, Web boot and client-manifest probe, package removal, restored-composition
dump and cleanup. It also passed independent boot/removal probes without the
Extension Center and without each of Task Monitor, Agent Market, Agent Builder,
Skill Market, Notification Center, platform Scheduler, User Settings and
Developer Resources. No legacy Session-reminder rows remained.

## 3080 browser acceptance

The live `http://127.0.0.1:3080/` surface was loaded after the final build.

- Paramont shell and native conversation remained usable.
- Settings exposed Extension Center, platform scheduled tasks, Agent Center,
  Skill Market, personalization and Developer Resources.
- Extension Center rendered seven product categories and real loaded status,
  without creating a second technical registry.
- Scheduled Tasks rendered definitions, run results and actions. Notification
  Center rendered unread state, artifacts, external actions and Session links.
- Agent Center rendered native platform presets; Skill Market rendered native
  discovery/install state.
- Developer Resources read 50 live PAIMind Loader rows: 50 running, zero
  loading, zero failed, zero disabled and zero unobserved.
- Browser console: zero errors and zero warnings after all interactions.
- Visible body scan: zero exact retired package identities.

## Upstream and residual sentinels

- DeepSeek Harness stayed at
  `47f943859bef60e4160492346772ded9b24f765a`, with tracked source clean. Its 11
  pre-existing untracked PPT artifacts retained content fingerprint
  `6b1d460224290038ba6928c44c7508e3fa6312c815581259fa45a2d92efe686c`.
- Better Sidebar review checkout stayed at
  `09de13c7bcd845c4742f7d15de2d114ec84cd341` and clean.
- Cordis remained inside the untouched Harness source sentinel.
- Disposable removal probes left no PAIMind configuration rows or temp profile;
  restoring the selected bundle returned the full composition.
- Port 3080 remained available after acceptance.

## Known risk and authorization boundary

The Office Viewer still declares a stale Better Sidebar peer range. The exact
selected combination is accepted because real composition and browser gates
passed; any provider upgrade must repeat the full promotion rule. Shared test
environment acceptance, production release, commit, push and publish remain
outside this local remediation.
