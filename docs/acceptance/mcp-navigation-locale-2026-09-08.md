# 连接入口语言修复

资源库连接入口原先将名称、说明、分组和可访问名称固定为中文。现订阅原生语言变化，英文显示 MCP、Connect external services and tools，归入 Capabilities & tools；设置菜单英文显示 MCP Center。中文入口保持原文。

本次范围为导航入口，不代表连接管理表单全部完成英文翻译。

22 项定向测试、完整构建和框架检查通过。实际 localhost:3080 英文浏览器资源库中已确认 MCP 与 Skills 同组，无独立中文工具分组，并查看当前截图验证显示。

## 通用连接中心与内部多语言

本轮补齐内部列表、筛选、详情、表单、确认弹层及产品诊断文案的英语适配。用户连接名、助手名与第三方原始诊断保留原文。

统一顶部添加入口，选择本地 stdio、远程 streamable-http 或来源插件模板。飞书移到模板区，由飞书插件贡献可选英文元数据，不在通用页面硬编码供应商名单。设计与能力边界见 `docs/plans/mcp-center-generic-design.md`。

验证：
- MCP 中心与飞书桥接共 19 项测试通过，含通用远程保存结构、稳定引用与删除影响确认；完整构建与类型检查、42 包接口快照、框架检查通过。
- 真实宿主组合安装、启动、卸载、恢复与独立插件组合检查通过。最终模板提示语言与单复数微调后重新通过定向测试和构建。
- localhost:3080 英文实际页面验证通用入口、远程空草稿、飞书模板草稿及英文说明，取消草稿后未创建新连接。
- 对“个人飞书 · 写作验收”执行协议测试，发现 4 个原生工具，仍引用“飞书写作助手 0907”。不以握手代替真实文档业务验收，未执行文档写入。
- 与变更前比较：连接仓库字节摘要一致；286 个智能体/预设文件内容无变化。三条连接标识、版本及配置保持不变。
- 当前能力仍为工具连接管理；未新增 OAuth、资源或提示模板管理，也未升级宿主协议版本。未发布。

## 连接测试即时反馈

- 测试与启用并测试显示进行中状态，禁用重复操作；完成后直接更新对应卡片。
- 卡片常驻展示最近测试状态、时间和具体结果；管理请求失败单独显示“测试未完成”，不把旧成功结果误报为本次成功。
- 15 项 MCP 定向测试通过，含延迟探测与失败结果在未展开详情时可见；构建、框架门禁通过。
- 2026-09-08 19:46 中文实际页面点击既有飞书连接测试，卡片直接显示“最近测试通过”、时间与发现 4 个工具。未调用业务工具，未更改既有连接配置或助手绑定。

## 保存前测试当前配置

- 添加与编辑表单的“保存连接”旁新增“测试连接”。共享同一份字段和 JSON 解析/契约校验，测试直接发送当前草稿，不需要正式连接或版本号。
- 测试期间显示状态并防止重复提交；结果在表单内展示，失败保留草稿。修改任一配置后旧结果失效，提示重新测试；取消不会保存。
- 新增管理接口 `probeDraft`，复用兼容包现有的临时协议连接、握手、工具发现与关闭流程。测试不写连接仓库或正式健康证据，不挂载工具，不更改助手引用，慢探测不阻塞原有助手协调队列。
- 25 项 MCP 与飞书定向测试通过，覆盖未保存本地/远程草稿、当前参数与凭证引用、失败重试、表单校验、中英结果、编辑后结果失效，以及原有连接状态和挂载保留。类型检查、完整构建、42 包接口快照、框架检查和真实宿主组合安装/启动/卸载验证通过。
- 2026-09-08 20:00 左右在 `localhost:3080` 实测新建本地草稿，填写不存在程序后直接测试，表单显示“当前配置测试失败”与本机路径提示；未保存，修改名称后显示“配置已修改，请重新测试”。
- 从飞书模板新建未保存草稿，直接测试通过并发现 4 个工具；已有“个人飞书 · 写作验收”的编辑表单也测试通过。全部取消，未保存测试草稿或修改配置；详情仍显示引用“飞书写作助手 0907”。已查看成功与失败页面截图。
- 浏览器验证后，既有连接配置文件摘要与变更前一致，286 个智能体/预设文件内容无变化。测试仅覆盖协议握手与工具发现，未执行飞书业务操作。仅更新本地 3080 运行实例，未发布。
