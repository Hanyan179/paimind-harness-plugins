# 产品主线收拢与上游更新核查

日期：2026-09-05。范围：产品线；企业多用户分支和企业改造草稿暂缓。

## 分支事实与整合决策

- 核查时共有 14 个本地分支、3 个远端分支。已执行 `git fetch origin`；远端没有新增产品提交。
- 除 `codex/enterprise-multiuser-foundation-20260827` 外，另外 13 个本地分支的已提交历史全部包含在 `main` 的 `5413c43` 中。企业分支另有 1 个未合入提交，本次不合入。
- `main` 成为唯一活动产品主线；已切换主工作目录。历史分支和工作目录保留，未进行删除或覆盖。
- 旧工作目录 `/Users/hansen/.codex/worktrees/2440/paimind-harness-plugins` 的 27 个已修改或未跟踪文件逐一按字节比较，全部与已合入提交 `009be26` 中对应文件相同，不应再应用旧补丁覆盖较新实现。
- 提案演示工作目录剩余未跟踪内容为演示上下文与产物（包括图片、视频、演示文稿、数据和网页），不作为遗漏的源码合入。
- 收拢当前 11 个已跟踪文件的会话恢复修复：被持久会话引用的智能体通过来源服务拒绝删除；内置模板只接受显式登记的历史回执摘要，仍检查来源与组合，拒绝未知摘要及伪造组合。保留现有产品架构说明，企业改造草稿不进入此次提交。

## 上游更新

通过各仓库 `fetch` 后的快进更新同步本地源码参考，未向第三方仓库推送：

| 对象 | 更新前 | 更新后 | 运行依赖状态 |
| --- | --- | --- | --- |
| DeepSeek Harness（宿主框架）`master` | `b150a551b8` | `d347e70390` | 主分支已有 `0.1.3-alpha.1` 标签；已发布默认通道为 `0.1.2-rc.1`，当前运行仍为 `0.1.1-rc.2` |
| Better Sidebar（侧边栏插件）`main` | `09de13c` | `a5c52b3` | 默认发布版 `0.18.0`；当前运行仍为 `0.17.1` |
| Office Viewer（办公文件查看器） | `0.1.2` | 查询到 `0.2.0` | 未升级；新版本声明侧边栏 `^0.17.0`，不覆盖 `0.18.0`，核查时也不足仓库要求的 24 小时发布龄 |

来源：[宿主仓库](https://github.com/deepseek-ai/deepseek-harness)、[侧边栏发布记录](https://github.com/omdsh-dev/DSH-better-sidebar/releases)、对应软件包的官方 npm registry 元数据。主分支源码同步不等于运行版本升级。

## 新版组合试装结果

在 `/tmp/paimind-upstream-candidate-20260905` 的独立工作目录中试装宿主 `0.1.2-rc.1`、侧边栏 `0.18.0` 与 Cordis（插件运行框架）`4.0.2`。安装成功，但类型检查失败，未通过升级门禁：

1. `@deepseek-ai/dsh-settings` 不再导出 `installSettingsSection`。
2. `@deepseek-ai/dsh-settings` 不再导出 `settingsNamespace`。
3. `foldScheduleEvents` 的第二个参数改为 `SessionLogOffset`，现有 `number` 不兼容。

阻断位置均在 `packages/harness-compat/src/host.ts`。新版还涉及设置注册和远程调用契约迁移，需要完成适配及组合、浏览器回归后才可改写选定版本。此次未将失败候选带入主线、未升级现有本地用户数据、未更新兼容性矩阵为通过。

## 验证

- `pnpm run check:fast` 通过：107 个测试文件、594 项测试，类型检查、构建、40 个包的打包和公开接口检查、框架与文档检查均通过。
- `node scripts/verify-harness-composition.mjs` 通过：原选定宿主、侧边栏与办公查看器组合的真实安装、启动、卸载、恢复及各主要插件缺席验证通过；上游源码无额外修改。
- 重启 `127.0.0.1:3080` 后，浏览器确认历史会话页面可加载，智能体中心返回 14 个个人智能体，模板中心返回 3 个内置模板及文件树。历史对话仍展示 9 月 3 日保存的失败提示；此次未重跑该轮模型调用，不能据入口检查宣称全部历史失败已恢复。
- 新版候选的类型检查失败属于升级阻断，不能计入现有组合的通过结果。
- 已知上游包缺失源码映射的警告仍存在，不据此宣称控制台零警告。

本次证据限于主线整合、当前修复和组合回归，不构成生产发布或全部业务流程验收。
