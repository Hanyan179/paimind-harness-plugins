# 资料库真实数据迁移与对话验收

2026-09-08，承接用户要求：迁入本地 Cloudflare OS（参考平台）的三个资料夹，通过真实模型对话验证是否访问原文、引用事实，并检查连接权限。文本资料访问与引用通过；PDF（便携文档）原始文件访问通过，**正文理解未通过，当前工具未提供正文解析**。不能将本次结果表述为全部文档理解通过。

## 环境与数据范围

代码在 `codex/context-library-workspace-editors` 分支，接续 `0ee7938`。实际运行在 `http://127.0.0.1:3087/`，使用独立宿主数据目录 `/private/tmp/paimind-context-acceptance-XkxjDr`。本轮未合并或切换日常 3080 服务，未生产发布。

来源为 `/Users/hansen/Documents/cloudflare-os` 的本地持久化资料集合及对应对象文件；源码提交 `b810d09f5358eeb4aeeef2acdde45aff54405277`，操作后工作副本仍无变化。源数据库以只读方式打开，按快照导出实际存储文件，不读取或迁移凭证。第四个“测试”集合为空，本次没有迁移。

| 原资料夹 | 实际迁移文件数 | 原始字节数 |
| --- | ---: | ---: |
| Walmart Trend Intelligence（沃尔玛趋势情报） | 9 | 56,369,434 |
| sku data（产品数据） | 355 | 23,868,202 |
| Trend Research Skills（趋势研究方法） | 26 | 2,191,593 |
| 合计 | 390 | 82,429,229 |

通过运行中插件的管理接口创建资料夹并写入，保留层级、原文件名、文件说明和资料夹说明；原有两个合成验收资料夹保留。每个文件均检查导出原件、写入回执及读取返回的版本，全部迁完和模型测试后，再逐个比较受管文件与导出原件的完整字节数及 SHA-256（内容摘要），390/390 一致。目录中的同步清单和隐藏文件也按原件保存。

迁移仅覆盖这三个集合实际保存的内容，不按正文中的外部链接抓取其他系统。产品来源自述是冻结快照，未重新核对飞书在线数据。迁入的 `SKILL.md` 仍是普通资料；没有注册为技能、执行其脚本或运行趋势分析流程。

原件、逐文件清单、迁移回执和完整对话证据保存在仓库外的本地目录：`/Users/hansen/Documents/PAIMind-workspace/context-library-migration-20260908`。原件在 `originals/`，实际运行中的资料文件在宿主目录的 `.paimind-context-library/collections/`；模型访问后者。验收宿主仍使用临时目录，不能将其当作日常数据归档；仓库外持久目录保留了可恢复原件与映射。业务正文、截图和本地宿主数据没有提交进代码仓库。

## 真实模型与连接

使用原有配置的 DeepSeek-V4-Flash（深度求索模型），提供方 `deepseek-official`，原生 `standard` 预设。没有创建模型响应替身。创建“真实资料访问验收”原生工作区，在浏览器的使用方资料面板将三个资料夹逐个选为只读，再从浏览器提交第一轮自然语言产品查询；其余用例通过宿主原生 `session.prompt` 提交，最终在浏览器中复核真实会话、工具展开结果和回答。

另建“未连接资料对照”原生工作区。它没有工作区资料连接：先查询，再临时增加一个只读会话连接，完成读取后解除，再次调用原工具验证拒绝。所有连接仍由资料库统一维护，未在工作区文件中复制资料或存储第二套连接列表。

实际完成 **5 个会话、9 轮对话、76 次工具调用**。从宿主历史提取并按调用编号关联参数和结果，记录模型提供方、会话、轮次、时间、文件路径及版本。全部调用只有 `skill` 和 `paimind_context`；未使用命令、其他文件工具或网络旁路。检查原生技能目录没有自动注册两个迁入研究技能。成功读取的 **24 次返回内容**逐次与迁移原件对应字节及版本摘要相同，回答发生在返回结果之后。

## 对话结果

| 场景 | 真实提问及验证 | 结果 |
| --- | --- | --- |
| 产品原文及引用 | 浏览器提问产品品名、售价、来源页码和目录产品数、分类数；提示中没有提供答案或文件路径。模型主动调用目录、搜索与读取，引用产品记录、目录索引、页面产物及具体字段；字段值与原件一致，分类口径明确包含未分类 | 通过 |
| 趋势资料状态 | 询问原始报告与可用的已发布分析。模型列出三份报告，读取根说明、处理契约、源清单和产物说明，正确区分示例配置与实际产物，报告当前没有已发布分析 | 通过 |
| 研究方法原文 | 询问两个阶段、输入输出、校验及失败能否替换发布指针。模型读取两个 `SKILL.md` 及相关契约，只解释方法；两阶段顺序、输入输出与失败保留旧指针的结论符合原文 | 通过 |
| 无连接对照 | 查询相同产品，工具目录返回空集合、搜索返回空结果，模型没有给出猜测的品名价格 | 通过 |
| 显式会话连接 | 为同一对照会话添加只读连接，模型重新检索和读取产品原文，得到正确字段及路径；没有经过智能体中心创建入口 | 通过 |
| 只读写入拒绝 | 要求模型实际调用一次创建新探测文件的 `write`，工具返回 `ACCESS_DENIED`；完整文件核对确认探测文件不存在、390 个原件未改变 | 通过 |
| 解除后的原工具访问 | 解除对照会话连接后，要求实际读取已知集合及原路径；工具返回 `ACCESS_DENIED`，模型只报告本次拒绝，不以历史正文代替新读取 | 通过 |
| 宿主冷重启 | 停止旧进程后用相同数据目录重新启动。进程开始时间为北京时间 21:28:28，位于第二轮完成与第三轮调用之间。恢复既有产品会话，新一轮确实再次调用 `read`，原文及版本相同 | 通过 |
| PDF（便携文档）文件访问 | 模型定位并实际读取 30,522,003 字节报告的前 2,048 字节，返回正确内容类型、完整版本摘要及对应 `%PDF-` 文件头 | 原始字节访问通过 |
| PDF（便携文档）正文理解 | 要求列出上述报告正文的两个趋势和页码。工具仅返回原始二进制的 Base64（字节编码），资料夹也无已解析产物。模型明确无法可靠回答，没有编造趋势或页码 | **未通过：当前不支持正文解析** |

只读、撤权检查只证明资料工具边界，未宣称操作系统级隔离；权限解除也不撤回历史消息。PDF（便携文档）正文用例未允许其他本地目录或网络访问，以直接检验资料工具自身能力。没有将模型的诚实拒绝算作正文理解成功，也没有在本轮测试中额外建设解析或扫描识别流水线。

重启后管理接口复核：每个迁入资料夹仍只有一条指向“真实资料访问验收”的只读工作区连接，对照会话的临时连接已解除。“被谁使用”只展示反向关系，当前工作区名称展示为路径尾段 `authorized-workspace`，未出现资料库中心授权控件。

## 本轮修复及工程检查

真实来源中的最大报告为 30.5 MB（兆字节），超过原先 20 MiB（20 × 1,024 × 1,024 字节）限制。资料库改为有界的 64 MiB（67,108,864 字节），请求编码长度与实际字节上限共同校验；分段读取仍最多 64 KiB（65,536 字节）。共享受管写入接口增加可选上限，版本检查的两次读取采用同一个上限；其他调用方继续使用原先默认值。

新增定向测试覆盖超过 20 MiB（20 × 1,024 × 1,024 字节）文件的创建、分段读取、版本更新、旧版本冲突、回收恢复，以及超过上限时拒绝且不创建文件。

| 检查 | 本轮结果 |
| --- | --- |
| 完整测试 | 135 个文件、760 项通过，37.32 秒；已有上游源码映射警告没有造成失败 |
| 定向测试 | 3 个文件、37 项通过，包含资料存储及工作区编辑回归 |
| 类型检查与全量构建 | 通过 |
| 包规范与发布包检查 | 44 个包通过；无业务数据、凭证、截图或测试污染 |
| 公开接口快照 | 首次正确报告两个包的四处摘要差异；审阅新增资料大小常量及共享写入可选参数后，只更新这两个包的运行入口和声明摘要；复核通过 |
| 入口、声明消费、客户端体积 | 严格包检查通过；44 包的 152 个公开导出消费通过；27 个客户端体积通过 |
| 框架、示例及源码来源 | 全部通过；来源清单与固定参考提交实际字节核对通过 |
| 真实宿主组合 | 完整安装、启动、移除、恢复与原有功能隔离检查通过，上游工作副本变化为零 |
| 浏览器 | 实际配置使用方连接、提交产品对话、展开读取与拒绝结果；冷重启后回读、资料夹及反向记录可见 |
| 文档链接 | 最终文档检查通过 |

## 复核入口与证据

浏览器打开 `http://127.0.0.1:3087/`，左侧“真实资料访问验收”下保留四个资料查询会话；“未连接资料对照”下保留完整的未连接、授权、撤权三轮对话。当前选中“产品数据原文验证（浏览器）”。

| 会话 | 宿主身份 |
| --- | --- |
| 产品数据原文验证（浏览器） | `session-4bf472ed-dfd6-4796-8415-bb7856f67062` |
| 趋势资料引用验证 | `session-34697853-8951-484a-941f-d73c9b00d583` |
| 研究方法原文验证 | `session-66a7de9d-b988-4daa-8852-79e63eb9784a` |
| PDF正文访问验证 | `session-09c34592-345a-4b3c-988d-f9bc6762f178` |
| 资料权限对照：未连接、授权、撤权 | `session-462512db-cd19-4fa9-9dcc-29c10eb5ea98` |

仓库外目录的 `manifest.json`、`import-receipt.json` 保存全部文件摘要与来源到目标的映射；`e2e-sessions.json` 保存真实工作区与会话身份；`evidence/` 保存脱除推理文本后的原生调用、工具结果、最终回答、连接变更和重启记录，`verification.json` 保存可重跑的核对结果。完整私有数据仅在本地查看。

浏览器截图为 `sku-answer.png`、`readonly-write.png`、`restart-tool-read.png`、`revoked-read.png`、`pdf-body-gap.png`、`migrated-folders-usage.png`，均在 `evidence/`。工程日志为 `/tmp/paimind-real-context-*.log`。上述文件不进入发布包或代码提交。

已保存证据可通过 [核对脚本](../../scripts/verify-context-migration-evidence.mjs) 重新验证；它只读取现存证据和文件，不发送新的模型任务：

```sh
node scripts/verify-context-migration-evidence.mjs \
  /Users/hansen/Documents/PAIMind-workspace/context-library-migration-20260908 \
  /private/tmp/paimind-context-acceptance-XkxjDr/.paimind-context-library
```

该脚本针对本次固定验收案例，核对完整文件字节、原生调用关联、逐次读取、产品事实与引用、权限拒绝和进程重启时间关系；**不能脱离其证据文件替代一轮新的真实模型验收**。其他三类编辑器、导出及原合成读写闭环仍以 [首次交付验收](context-library-workspaces.md) 为准，不将本轮资料测试扩大为编辑器全场景重验。
