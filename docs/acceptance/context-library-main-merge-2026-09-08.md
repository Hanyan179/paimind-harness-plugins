# 资料库与工作区主线合并验收

2026-09-08，用户接受现有文件正文解析边界，并明确授权合并到本地主分支。本次将 `codex/context-library-workspace-editors` 的 `515daf8` 合入 `main` 的 `bca4b00`，保留主线已有品牌设置、连接中心与提案预览变更。没有推送远端或生产发布。

## 合并与集成修复

唯一文本冲突为公开接口快照，逐包逐字段合并两侧契约；主线与功能分支同时修改的 `harness-compat` 和 `mcp-center` 在全量构建后重新审阅对应运行入口、声明摘要，没有整份覆盖任意一侧。

日常 3080 宿主验证发现，新插件界面入口可以显示，但资料库管理接口返回 404。资料库和工作区编辑器的 `./typert` 当时只转出客户端声明，缺少原生加载器读取的具名 `TYPERT` 宿主声明。补齐两个插件的完整声明，与客户端继续共享同一份方法描述，避免依赖特定模块安装布局下的运行时标记。

增加两个注册回归测试，并增强真实组合检查：除客户端清单外，必须实际调用资料库集合查询和工作区编辑器接口；后者使用不存在的工作区，必须收到业务层“工作区不存在”，不能把入口存在或 HTTP 首页成功当作接口可用。修复后日常主线实际集合查询与已有合法工作区的文档列表查询均成功，公开接口快照仅更新这两个插件新增宿主声明所对应的摘要。

## 日常运行数据

日常服务为 `http://127.0.0.1:3080/`，使用主线目录内既有 `.dsh-home`。通过宿主插件安装命令增加资料库与工作区编辑器两个本地包；逐项核对既有依赖、配置层及用户补丁保留。确认没有运行中的模型会话后重启加载合并代码。重启前后原有 **303 个会话身份和 15 个工作区记录一致**。

按此前用户授权，从仓库外保存的原始迁移快照将三个实际资料夹迁入日常主线资料库。通过资料库管理接口创建和写入，保留目录、名称、说明、内容类型；未复制验收环境中的合成资料、工作区或会话，没有将验收连接自动授予主线智能体或工作区。使用方可自行选择资料及只读、读写权限。

| 资料夹 | 文件数 | 原始字节数 |
| --- | ---: | ---: |
| Walmart Trend Intelligence（沃尔玛趋势情报） | 9 | 56,369,434 |
| sku data（产品数据） | 355 | 23,868,202 |
| Trend Research Skills（趋势研究方法） | 26 | 2,191,593 |
| 合计 | 390 | 82,429,229 |

逐文件检查写入回执、读取返回的大小及版本，并再次直接读取日常资料库受管文件核对完整 SHA-256（内容摘要）；**390/390 一致**。三个资料夹的反向连接均为空。原始迁移快照仍保留，主线文件存于 `.dsh-home/.paimind-context-library/collections/`。

## 验证结果与范围

| 检查 | 本次结果 |
| --- | --- |
| 完整测试 | 137 个测试文件，772 项通过；已有上游源码映射警告没有造成失败 |
| 类型检查、全量构建 | 通过 |
| 包规范、打包、严格包入口检查 | 44 个包通过 |
| 公开接口、声明消费、客户端体积 | 通过；152 个公开导出、27 个客户端包 |
| 示例、框架、参考源码来源 | 通过 |
| 真实宿主组合 | 安装、启动、移除、恢复及原有功能隔离通过；新增真实后端接口检查通过；上游工作副本变化为零 |
| 日常主线接口 | 集合查询成功；合法既有工作区的文档列表返回成功；原有会话和工作区保留 |
| 日常浏览器 | 收起已有工作面板后，从资源库打开资料库，三个资料夹可见；打开实际 `README.md`，正文与原件一致；模板中心显示原有三套和新增文档、幻灯片、表格三套模板 |
| 文档链接、差异空白检查 | 通过 |

本次属于合并后的集成回归，没有重新发送完整模型验收任务。此前真实 **5 个会话、9 轮、76 次工具调用、24 次成功原文读取**及授权、撤权、重启证据保留在[真实数据对话验收](context-library-real-data-e2e.md)；三类编辑器完整操作和导出证据见[首次交付验收](context-library-workspaces.md)。本次浏览器模板入口检查不替代这些完整场景，也不把旧证据标成主线新发起的模型对话。

## 已接受的限制

用户接受的限制继续保留：`raw/reports/mik_trends_2026.pdf` 的 30,522,003 字节完整保存、原始字节可访问，但资料工具尚未解析报告正文，不能可靠回答正文趋势和页码。此前该正文理解用例仍是未通过，不因用户接受或合并而改成通过。另两份报告 `Back_to_School_A_W_27_28_en.pdf`、`Buyers__Briefing_S_S_27__Gifting_en.pdf` 同样不具备正文解析能力，未将它们表述为逐份完成真实模型正文测试。

## 私有证据

业务文件、宿主数据、截图和配置不进入提交或发布包。主线导入映射为仓库外 `context-library-migration-20260908/import-receipt-main-3080.json`，保留原 3087 验收回执。

合并验证记录位于 `/private/tmp/paimind-context-main-merge-20260908/`：`native-before.json`、`native-after.json`、`runtime-upgrade.json`、`import-verification.json`，以及 `library-main.png`、`library-read-main.png`、`templates-main.png`。工程日志为 `/tmp/paimind-context-main-*-final.log` 及 `/tmp/paimind-context-main-gates-final-*.log`。这些临时证据不作为业务归档目录，日常资料原文件已经进入主线持久宿主目录。
