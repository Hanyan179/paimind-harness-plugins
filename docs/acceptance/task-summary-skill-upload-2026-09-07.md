# 任务摘要修复与写作技能上传、绑定验收

日期：2026-09-07。分支 `codex/mcp-center-feishu`，基线 `d7078f5`。本轮修改、构建和真实浏览器验收已执行，未提交或发布。范围承接[连接中心计划](../plans/mcp-center-feishu.md)中的补充评审及 [RQ-108（任务摘要需求）](../product/RQ-108-task-summary-prd.md)。

## 任务摘要修复

Task Monitor（任务监控）通过来源中心的公开只读契约读取智能体名称、技能与连接引用；连接中心提供本用户连接的名称、原生命名空间与当前挂载状态。配置和运行状态仍各自由原所有者提供；任务监控只保存临时展示数据，没有新增工具调用入口、凭证副本或持久化注册表。

- 当前智能体显示“飞书写作助手 0907”，不再将 `0907-21fcf6` 当作名字。
- “智能体、技能与连接”分别显示当前配置和本会话证据。未调用也能看到已挂载连接；技能实际成功加载后显示“已挂载 · 已加载”，连接实际使用后才显示“已使用”。
- 已停用连接保留历史使用事实，同时显示停用状态；解绑且从未使用的连接不出现在当前摘要。失效引用和无法解析名称的受管连接不暴露内部散列标识。
- 来源插件是可选依赖。独立失败、缺席或卸载只影响对应展示；面板打开期间每五秒刷新，切换会话时丢弃旧会话的异步结果。

## 完整技能提取与真实上传

本机源目录为 `/Users/hansen/.codex/skills/human-writing`，版本 `1.1.0`。完整归档包含 11 个文件、展开后 92,901 字节，包括权威说明、全部参考文档、脚本、许可证、版本、界面说明及轻量版本资源。没有只提取 `SKILL.md`，没有改写原资源，也没有创建第二个技能名称。

压缩包为 `human-writing-codex-1.1.0.zip`，44,755 字节，SHA-256（文件校验值）为 `d6bfadb4ba9ad9a3f77c2ad623e85d68174bfb7baa915a84076ff96208e118f6`。

在完整的 `http://127.0.0.1:3080/` 中实际点击“技能中心 → 添加 → 从设备导入”，通过浏览器文件选择器提交压缩包，再检查“更新 human-writing”预览并确认。成功提示为“human-writing 已原子更新；失败时旧版本会自动回退。”安装后的 11 个资源逐一比对大小与校验值，全部与本机源文件相同，来源文件名也已记录为本次压缩包名称。

技能中心此前已存在同名且内容相同的安装，因此本轮验证的是上传更新流程，不能算首次安装验收。技能保持“允许使用”开启，“普通对话默认使用”和“用于当前对话”关闭。受管仓库的写入全部由上传流程完成，没有直接复制文件来绕过产品入口。

## 智能体中心绑定往返

从技能详情点击“从智能体中心挂载”，搜索准确名称“飞书写作助手 0907”后进入编辑。先取消该写作技能并保存，再关闭、重新打开、搜索技能并重新勾选保存，最终再次打开确认。

| 保存状态 | 版本 | 业务技能 | 工具连接 |
| --- | --- | --- | --- |
| 本轮开始 | 7 | `human-writing`、`lark-doc`、`lark-shared` | 个人飞书 · 写作验收 |
| 解绑并重新打开 | 8 | `lark-doc`、`lark-shared` | 个人飞书 · 写作验收 |
| 重新绑定并再次打开 | 9 | `lark-doc`、`lark-shared`、`human-writing` | 个人飞书 · 写作验收 |

逐字段核对两次保存，仅技能引用、版本和更新时间变化；角色、目标、行为、人设补充及连接引用均保留。最终技能集合与开始时相同，列表顺序变化。未保存其他智能体。

## 原生会话与浏览器证据

沿用原数据目录与完整六组产品插件，重启到本次构建后，从智能体中心启动第 9 版助手的真实会话 `session-92c0ba68-214e-486a-a415-6b5d2d756c1f`。验收标记为 `HW_UPLOAD_0907`。

第一轮明确要求只回复“已就绪”，原生日志中工具调用为零。此时浏览器摘要已显示正确助手名称、三项已挂载技能和“个人飞书 · 写作验收”，没有把配置显示成已经使用。

第二轮通过浏览器发送加载写作技能并创作短篇虚构片段的请求。原生日志 `seq=310` 调用 `skill`，参数为 `{"name":"human-writing"}`；`seq=311` 的结果 `isError=false`，资源根目录指向技能中心受管目录，返回内容包含与本机源文件一致的完整技能正文。浏览器随后显示“human-writing：已挂载 · 已加载”。该会话只执行了这一次工具调用。

原生请求目录中包含四个飞书工具，本次飞书调用数和文档写入数均为零；连接状态保持“已挂载”。本轮证明上传、绑定、原生加载与摘要状态流转，没有重新执行飞书文档写入验收，也没有完整评估文章质量或所有参考文档的按需读取。

绑定版本变化使原标签页准备了未发送的续接摘要草稿；没有自动提交或重放。浏览器名称与状态验收使用上述新会话，未将旧历史会话的原截图宣称为已直接复测。

## 工程验证与剩余失败

- 定向测试 7 个文件、32 项全部通过，包括名称解析、未使用资源、来源缺席和失败、停用历史、跨会话异步结果隔离以及连接摘要无探测副作用。
- 类型检查、生产构建及包规范、体积、公共接口快照、打包、严格发布检查、外部类型消费、示例、框架检查通过。公共接口基线只更新本次确有变化的两个包。
- 完整测试 626 项中 625 通过、1 项失败。失败仍为本轮未修改的 `packages/workspace-blueprints/tests/materializer.spec.ts:63`，原因是原模板缺少未跟踪的 `docs`、`outputs` 空目录；没有改动不可变模板或降低断言来掩盖失败。
- 真实 Harness（运行底座）组合通过完整安装、启动、卸载、恢复，以及缺少任务监控、智能体中心、技能中心等独立组合检查；上游源码改动为零。

[结构化证据](evidence/task-summary-skill-upload-2026-09-07.json) 包含逐文件校验值、绑定版本、原生调用标识与检查结果。压缩包、三张已目视检查的浏览器截图和检查日志保存在任务产物目录 `/Users/hansen/.codex/visualizations/2026/09/07/01a079c7-204f-7d50-8b82-a65499caeda5/task-summary-skill-upload`，没有加入仓库或发布包。
