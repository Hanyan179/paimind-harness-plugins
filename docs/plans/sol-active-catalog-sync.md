# SOL 有效商品资料同步

用户要求组合现有飞书连接、原生会话、定时任务与资料库，将 SOL 中 `Item Status = ACTIVE` 的商品生成 HTML（网页）资料，变更时更新，供其他智能体引用。计划每天北京时间 09:00 执行，先完成手动验收。

## 业务规则与来源

来源为用户指定的飞书多维表，SOL 为 `tbl6Fvd5kVAe74Ms`，同一表格空间的 `Artwork & Sample Tracker` 为 `tblEADKOj0TZlHrm`。用户提供的 `product-ppt-generator.zip`（v10）定义事实与规划规则：按去除首尾空格后的 `ITEM#` 精确关联；只用 `PRD Render Image`；每商品一页；无图留白；图片按数量等权放置；金额、字段显隐、IMU（初始加价率）60% 阈值颜色沿用来源。网页渲染消费同一构图规范。提示词不承担筛选、关联、排版或变更判断。

2026-09-09 实际读取 SOL 共 251 条，其中 ACTIVE 为 68 条；记录 `recvrXaBHfi4OS` 缺商品编号和名称。用户明确确认缺字段、缺图片不阻断：68 条全部保留，缺失值如实标记；低于 60% 标红，达到 60% 标绿（比较未经四舍五入的值）。源数据不写回。

## 所有权与冲突评审

| 维度 | 决定 |
| --- | --- |
| 产品入口 | 复用连接中心、定时任务、原生会话和资料库，不新增产品中心 |
| 业务规则与产物 | 仓库内独立业务工作流脚本拥有配置、确定性处理和产物版本；薄业务技能只负责调用顺序和验收，不承担业务判断 |
| 飞书访问 | 扩展既有 `feishu-cli-mcp`，增加通用多维表快照与附件下载；固定已有账号配置，只读远程数据，导出限于明确配置的本地目录及来源范围 |
| 调度与会话 | 复用既有智能任务动作与原生会话，指定已保存的飞书同步助手、真实工作区；不建立另一套调度器或调用接口 |
| 资料与授权 | 由资料库管理连接、版本和读写；生成工作区连接专属资料夹为读写，其他智能体自行选择只读或读写 |
| 发布 | 脚本生成一份自包含网页，文件开头嵌入可供模型读取的完整事实；原生资料工具按运行开始时读取的版本原子入库。每次都核对目标版本，无变化不改写；内容寻址候选文件不依赖第二份已发布状态。原图仅保存工作区，压缩副本用于网页 |
| 失败与生命周期 | 数据不完整、图片下载失败、重复关联冲突或入库失败保留上次成功版本，运行记录明确需处理；停用已有插件即停止相关能力，不删除历史产物 |
| 宿主兼容 | 不修改上游，不增加原生状态的影子副本；文件安全复用 `harness-compat/managed-files`，工具执行仍走原生会话 |

## 单任务文件授权

2026-09-09 用户明确授权“仅这个同步任务创建的会话使用完整文件访问”。授权保存在该任务已有动作输入的可选 `permissionPreset`，由 `harness-compat` 调用原生权限服务应用到新会话，不修改全局默认权限，不绕过资料库连接或版本检查。任务详情显示权限。模型调度工具不能通过输入为新任务增加此权限。原生权限服务不存在或应用失败时不启动执行。

真实试运行同时发现，原调度器创建的会话虽有目录却没有原生工作区归属。已通过原生工作区 `attachSession` 在首轮执行前登记，资料库只读取真实归属，不以目录猜测权限。

## 验收

验证真实飞书读取与附件来源、首次生成与入库、相同快照不更新、合成变更的增改与退出 ACTIVE、版本冲突及失败不发布、定时任务手动运行真实会话、其他只读会话引用产物。业务数据与凭证不提交；保留来源摘要、脚本测试、真实工具与浏览器证据。网页验收独立报告，不将网页生成宣传为 PPTX（演示文稿）完整符合性验收。
